<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.icecreampicturebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Icecreampicturebook">
        <activity android:name=".frenchcream"></activity>
        <activity android:name=".raspberrycream" />
        <activity android:name=".ubecream" />
        <activity android:name=".cookies" />
        <activity android:name=".raspberry" />
        <activity android:name=".almondActivity" />
        <activity android:name=".ubeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>